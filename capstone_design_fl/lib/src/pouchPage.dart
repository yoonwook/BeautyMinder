import 'package:flutter/material.dart';

class PouchPage extends StatelessWidget {
  const PouchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a Pouch Page'),
    );
  }
}